# Raw Redis connection string (NOT the REST URL used by the TypeScript backend)
# Format: rediss://default:<password>@<host>:<port>
# Get this from Upstash console → your database → "Connect" → "Redis CLI" section
UPSTASH_REDIS_URL=""
